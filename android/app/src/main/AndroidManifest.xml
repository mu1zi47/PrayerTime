<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed to fetch prayer times from the Aladhan API. Debug/profile
         builds get this for free from Flutter's own debug manifest, but
         release builds only see this main manifest — without it here,
         every network call fails with SocketException in release builds. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Needed for the "detect automatically" button on the city picker.
         Coarse only: the city is all this resolves to, and asking for the
         precise location would need a separate justification on Play. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Needed to schedule/show prayer-time notifications. SCHEDULE_EXACT_ALARM
         is the one the user grants; USE_EXACT_ALARM (granted automatically) is
         deliberately not declared — Play restricts it to alarm-clock and
         calendar apps, and requesting it would put the listing at risk. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="Prayer times"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Reschedules pending prayer-time alarms after a reboot or app
             update, since AlarmManager entries don't survive those. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>

        <!-- Handles taps on a notification's action buttons (e.g. the
             "Прочитал"/"Done" button on a prayer notification) — without
             this, the button shows but tapping it does nothing, since the
             PendingIntent it fires has no declared receiver to catch it. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"/>

        <!-- Home-screen widget: current prayer, next prayer, countdown and a
             "prayed" button (see PrayerWidgetProvider). Unexported on
             purpose — the system delivers APPWIDGET_UPDATE to it directly,
             and everything else it reacts to is an explicit intent this app
             sends itself, so no other app can drive its "mark as prayed"
             action. -->
        <receiver
            android:name=".widget.PrayerWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Without these, Android 11+ hides mail and browser apps from the
             package visibility check, and the feedback rows in Settings
             would silently fail to open anything. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
        <!-- Telegram's own scheme, so the feedback row opens the app rather
             than a browser tab pointing at t.me. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tg"/>
        </intent>
    </queries>
</manifest>
