package uz.mu1zi47.prayertime.widget

/**
 * The widget the picker offers at 4x1: the current prayer, the next one, a
 * countdown and the "prayed" button.
 *
 * Nothing but its placement size distinguishes it from its two siblings —
 * resize it and it takes their shape, see [BasePrayerWidgetProvider].
 */
class PrayerWidgetProvider : BasePrayerWidgetProvider()
