<?xml version="1.0" encoding="utf-8"?>
<!-- Placed at 4x2: a header line plus six slots need the second row of
     cells. Shrink it and it turns into the 4x1 or 2x1 shape instead — see
     BasePrayerWidgetProvider. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_day_description"
    android:initialLayout="@layout/prayer_day_widget"
    android:minHeight="110dp"
    android:minResizeHeight="40dp"
    android:minResizeWidth="110dp"
    android:minWidth="250dp"
    android:previewImage="@mipmap/ic_launcher"
    android:previewLayout="@layout/prayer_day_widget_preview"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="4"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
